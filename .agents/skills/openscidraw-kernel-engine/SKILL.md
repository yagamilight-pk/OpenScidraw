---
name: openscidraw-kernel-engine
description: High-performance local-first WebRTC CRDT state synchronization and client-side chemical vector rendering engine for scientific visualization workspaces.
category: Domain Skills
---
# OpenSciDraw Kernel Execution Procedures
- Trigger Condition: When changes are detected inside `src/kernel/*` or when the user initiates collaborative networking sessions.
- Ingestion Context: Ensure `src/kernel/stateEngine.ts` and `src/kernel/chemRenderer.ts` are linked directly into the principal workspace state loop.
- Execution Checklist:
  1. Boot compilation routines to verify zero runtime type-safety mismatch errors.
  2. Verify that conflict resolution string sorting prioritizes operations deterministically.
  3. Ensure chemical ring coordinate formulas generate fully bounded boxes capable of drop-scaling modifications.
